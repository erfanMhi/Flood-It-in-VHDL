library IEEE;
use IEEE.STD_LOGIC_1164.all;

package game_lib is

	type game_states is (input_catch, pre_start, started, lost, win);
	type color_data is array (0 to 3) of std_logic_vector(11 downto 0);

end game_lib;

package body game_lib is

end game_lib; 